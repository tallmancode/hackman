export default defineAppConfig({
    ui: {
        primary: 'red',
        light: 'light',
        button: {
            default: {
                size: 'md',
                color: 'gray',
                variant: 'ghost'
            }
        }
    }
})
